# =============================================================================
# 高考语文议论文自动出题-求解-评估系统 依赖清单
# =============================================================================

# -----------------------------------------------------------------------------
# 核心依赖（必需）
# -----------------------------------------------------------------------------

# LLM API 客户端
openai>=1.0.0                 # OpenAI API / 兼容接口（如 DMX、DeepSeek）

# 数据验证
pydantic>=2.0.0               # 数据结构验证（Schema 定义）

# -----------------------------------------------------------------------------
# LLM 提供商 SDK（按需安装，根据使用的模型选择）
# -----------------------------------------------------------------------------

# Google Gemini（使用 Google 模型时需要）
# google-genai>=0.1.0         # pip install google-genai

# Anthropic Claude（使用 Claude 模型时需要）
# anthropic>=0.18.0           # pip install anthropic

# -----------------------------------------------------------------------------
# 可选依赖（增强功能）
# -----------------------------------------------------------------------------

# 进度条显示
tqdm>=4.0.0                   # 批量处理进度显示

# 环境变量管理（旧版兼容，新版推荐直接配置 api_config.py）
# python-dotenv>=1.0.0        # 从 .env 文件加载环境变量

# -----------------------------------------------------------------------------
# 开发依赖（测试、代码质量）
# -----------------------------------------------------------------------------

# pytest>=7.0.0               # 单元测试框架
# pytest-cov>=4.0.0           # 测试覆盖率
# black>=23.0.0               # 代码格式化
# isort>=5.0.0                # import 排序
# mypy>=1.0.0                 # 类型检查

# =============================================================================
# 快速安装
# =============================================================================
#
# 基础安装（OpenAI 兼容接口）：
#   pip install -r requirements.txt
#
# 使用 Google Gemini：
#   pip install -r requirements.txt google-genai
#
# 使用 Anthropic Claude：
#   pip install -r requirements.txt anthropic
#
# 完整安装（所有 LLM 提供商）：
#   pip install -r requirements.txt google-genai anthropic
#
# =============================================================================
